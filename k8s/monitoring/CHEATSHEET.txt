╔══════════════════════════════════════════════════════════════════╗
║                  MONITORING QUICK REFERENCE                       ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ 🚀 DEPLOY                                                         │
└──────────────────────────────────────────────────────────────────┘
  cd backend && npm install
  cd ../k8s/monitoring && ./deploy-monitoring.sh
  cd .. && kubectl apply -f backend.yaml

┌──────────────────────────────────────────────────────────────────┐
│ 🌐 ACCESS                                                         │
└──────────────────────────────────────────────────────────────────┘
  Grafana:
    kubectl port-forward -n velora-wear svc/grafana 3000:3000
    http://localhost:3000 (admin/admin123)

  Prometheus:
    kubectl port-forward -n velora-wear svc/prometheus 9090:9090
    http://localhost:9090

  Backend Metrics:
    kubectl port-forward -n velora-wear svc/velora-backend-service 5000:5000
    curl http://localhost:5000/metrics

┌──────────────────────────────────────────────────────────────────┐
│ 📊 METRICS                                                        │
└──────────────────────────────────────────────────────────────────┘
  http_requests_total          - Total HTTP requests
  http_request_duration_seconds - Request duration
  orders_created_total         - Orders created
  products_viewed_total        - Product views
  users_registered_total       - User registrations
  nodejs_heap_size_used_bytes  - Memory usage

┌──────────────────────────────────────────────────────────────────┐
│ 🔍 LOG QUERIES                                                    │
└──────────────────────────────────────────────────────────────────┘
  All logs:      {namespace="velora-wear"}
  Backend only:  {app="velora-backend"}
  Errors:        {app="velora-backend"} |= "ERROR"
  Orders:        {namespace="velora-wear"} |= "order"

┌──────────────────────────────────────────────────────────────────┐
│ 🔧 TROUBLESHOOT                                                   │
└──────────────────────────────────────────────────────────────────┘
  Status:    kubectl get pods -n velora-wear
  Logs:      kubectl logs -n velora-wear <pod-name>
  Describe:  kubectl describe pod -n velora-wear <pod-name>
  Events:    kubectl get events -n velora-wear --sort-by='.lastTimestamp'
  Targets:   http://localhost:9090/targets (via Prometheus port-forward)

┌──────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                  │
└──────────────────────────────────────────────────────────────────┘
  Quick Start:  k8s/monitoring/QUICKSTART.md
  Full Docs:    k8s/monitoring/README.md
  Summary:      MONITORING_SETUP.md
  Examples:     backend/examples/metrics-usage-examples.js

┌──────────────────────────────────────────────────────────────────┐
│ ✅ VERIFY SETUP                                                   │
└──────────────────────────────────────────────────────────────────┘
  1. kubectl get pods -n velora-wear -l 'app in (prometheus,grafana,loki,promtail)'
  2. curl http://localhost:5000/metrics (via port-forward)
  3. Open Grafana → Dashboards → "Velora Wear - Backend Metrics"
  4. Grafana → Explore → Loki → {namespace="velora-wear"}

╔══════════════════════════════════════════════════════════════════╗
║  Need help? Check k8s/monitoring/README.md for detailed docs    ║
╚══════════════════════════════════════════════════════════════════╝
