# API Configuration
# Vite environment variables must be prefixed with VITE_
# Vite Environment Variables (Local Development)
# For production, edit public/env.js instead (no rebuild required)

VITE_API_URL=http://localhost:5000/api

